{{- /*
Tuwunel 是 Matrix homeserver，即房间、成员关系和消息的权威存储。使用 StatefulSet
和 PVC 是因为 Pod 重建后仍需保留这些状态；Cinny 只是读取/发送消息的网页界面。
Controller/Manager/Worker 都通过下方 Service 与它通信，而不是彼此直接传聊天消息。
*/ -}}
{{- if eq .Values.matrix.provider "tuwunel" }}
{{- $comp := dict "root" . "component" "tuwunel" }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "agentteams.tuwunel.fullname" . }}
  namespace: {{ include "agentteams.namespace" . }}
  labels:
    {{- include "agentteams.component.labels" $comp | nindent 4 }}
spec:
  serviceName: {{ include "agentteams.tuwunel.fullname" . }}
  replicas: {{ .Values.matrix.tuwunel.replicaCount }}
  selector:
    matchLabels:
      {{- include "agentteams.component.selectorLabels" $comp | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "agentteams.component.selectorLabels" $comp | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: tuwunel
          image: "{{ .Values.matrix.tuwunel.image.repository }}:{{ .Values.matrix.tuwunel.image.tag }}"
          imagePullPolicy: {{ .Values.matrix.tuwunel.image.pullPolicy }}
          ports:
            - name: matrix
              containerPort: 6167
          env:
            - name: CONDUWUIT_DATABASE_PATH
              value: {{ .Values.matrix.tuwunel.persistence.mountPath | quote }}
            - name: CONDUWUIT_ADDRESS
              value: "0.0.0.0"
            - name: CONDUWUIT_PORT
              value: "6167"
            - name: CONDUWUIT_ALLOW_REGISTRATION
              value: "true"
            - name: CONDUWUIT_ALLOW_LEGACY_MEDIA
              value: "true"
            - name: CONDUWUIT_ALLOW_UNSTABLE_ROOM_VERSIONS
              value: "true"
            - name: CONDUWUIT_DB_POOL_WORKERS_LIMIT
              value: "32"
            # Keep Matrix profile display names equal to AgentTeams identities by
            # default; Tuwunel otherwise appends its built-in "💕" suffix.
            - name: CONDUWUIT_NEW_USER_DISPLAYNAME_SUFFIX
              value: {{ .Values.matrix.tuwunel.newUserDisplayNameSuffix | default "" | quote }}
            - name: CONDUWUIT_SERVER_NAME
              value: {{ include "agentteams.tuwunel.serverName" . | quote }}
            - name: CONDUWUIT_REGISTRATION_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "agentteams.secretName" . }}
                  key: AGENTTEAMS_REGISTRATION_TOKEN
            - name: CONDUWUIT_REGISTRATION_SHARED_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "agentteams.secretName" . }}
                  key: AGENTTEAMS_REGISTRATION_TOKEN
            # Increase default cache capacity to prevent RocksDB thrashing
            # (tuwunel#123); matches embedded's start-tuwunel.sh default.
            - name: CONDUWUIT_CACHE_CAPACITY_MODIFIER
              value: "2.0"
            # Agent lifecycle cleanup safety net: collapse rooms once
            # their last local member leaves and force a /forget on
            # leave, so a recreated same-named worker/manager/human
            # starts with no inherited room state. See agentteams-controller
            # LeaveAll*Rooms / Delete*Room flows.
            - name: CONDUWUIT_DELETE_ROOMS_AFTER_LEAVE
              value: "true"
            - name: CONDUWUIT_FORGET_FORCED_UPON_LEAVE
              value: "true"
            {{- range $k, $v := .Values.matrix.tuwunel.extraEnv }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
          resources:
            {{- toYaml .Values.matrix.tuwunel.resources | nindent 12 }}
          readinessProbe:
            tcpSocket:
              port: matrix
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: matrix
            initialDelaySeconds: 10
            periodSeconds: 20
          volumeMounts:
            - name: data
              mountPath: {{ .Values.matrix.tuwunel.persistence.mountPath | quote }}
      {{- if not .Values.matrix.tuwunel.persistence.enabled }}
      volumes:
        - name: data
          emptyDir: {}
      {{- end }}
  {{- if .Values.matrix.tuwunel.persistence.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: {{ .Values.matrix.tuwunel.persistence.size | quote }}
        {{- if .Values.matrix.tuwunel.persistence.storageClassName }}
        storageClassName: {{ .Values.matrix.tuwunel.persistence.storageClassName | quote }}
        {{- end }}
  {{- end }}
{{- end }}
